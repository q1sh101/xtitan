{
  "name": "@xtitan/monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "XTitan - Cross-platform monorepo (Web, Android, iOS)",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "keywords": [
    "monorepo",
    "react",
    "react-native",
    "expo",
    "vite",
    "typescript"
  ],
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}